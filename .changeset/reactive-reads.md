---
"drop-action": minor
---

Add reactive read API: `useActive()` (id, data, status, origin rect), `useOver(zoneId)`, `useItem().isDragging`, and a `container` prop on `Active` to override the portal target.
